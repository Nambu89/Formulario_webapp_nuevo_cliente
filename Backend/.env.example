# ─────────────────────────────────────────────────────────────
# Backend environment example.
# Copy this file to .env and fill in your values.
#   cp .env.example .env
# ─────────────────────────────────────────────────────────────

# Database connection string (SQLAlchemy format).
# Examples:
#   Azure SQL (with Entra ID service principal):
#     mssql+aioodbc://<server>.database.windows.net/<database>?driver=ODBC+Driver+18+for+SQL+Server&Encrypt=yes
#   Local SQL Server:
#     mssql+pyodbc://localhost/<database>?driver=ODBC+Driver+18+for+SQL+Server&Encrypt=no
#   PostgreSQL (async):
#     postgresql+asyncpg://user:password@localhost:5432/<database>
DATABASE_URL=mssql+aioodbc://your-server.database.windows.net/your-database?driver=ODBC+Driver+18+for+SQL+Server&Encrypt=yes

# ── Microsoft Entra ID (optional — only if using service-principal DB auth) ──
# If your DATABASE_URL already embeds credentials (user:password@host),
# leave these blank. They are only used for ActiveDirectoryServicePrincipal auth.
AZURE_CLIENT_ID=
AZURE_TENANT_ID=
AZURE_CLIENT_SECRET=

# ── JWT ──────────────────────────────────────────────────────
# Generate a strong random secret, e.g.:
#   python -c "import secrets; print(secrets.token_urlsafe(48))"
JWT_SECRET_KEY=change-me-to-a-long-random-string
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# ── CORS ─────────────────────────────────────────────────────
# Comma-separated list of allowed origins for the frontend.
# In production, set this to your frontend URL(s).
CORS_ORIGINS=http://localhost:3000,http://localhost:5173,http://127.0.0.1:3000,http://127.0.0.1:5173

# ── Azure Storage (optional — for blob document uploads) ────
AZURE_STORAGE_CONNECTION_STRING=
AZURE_STORAGE_CONTAINER_NAME=documents

# ── Application Insights (optional) ─────────────────────────
APPLICATIONINSIGHTS_CONNECTION_STRING=

# ── Server ───────────────────────────────────────────────────
# Host to bind. Use 127.0.0.1 for local dev, 0.0.0.0 inside Docker.
HOST=127.0.0.1
PORT=8000
ENVIRONMENT=development
UVICORN_LOG_LEVEL=info

# Optional one-time admin bootstrap.
# Leave both empty to disable the seed by default.
SEED_ADMIN_EMAIL=
SEED_ADMIN_PASSWORD=
SEED_ADMIN_NAME=Administrator

# Optional local-only password for demo seed scripts.
DEMO_USER_PASSWORD=
